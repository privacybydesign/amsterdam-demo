{
    "url": "<exposed ip:port of api-server (proxy)",
    "no_auth": false,
    "requestors": {
        "openstad_voting_pk": {
            "auth_method": "publickey",
            "key_file": "./config/di.vote.public.pem"
        }
    }
}

